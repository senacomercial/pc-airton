{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd /app/backend && npm ci --include=dev && npm run build"
  },
  "deploy": {
    "startCommand": "sh -c 'cd /app/backend && npm run db:deploy && npm run start:prod'",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}
